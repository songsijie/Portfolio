1️⃣是文档，通过2分块(chunk)，然后将分块后的文本embedding向量化，将分块文本，向量，文档元数据等信息都记录下来存到向量数据库里，一般是milvus + es。5️⃣里就是拼接上下文提示词，最后一起提供给llm等他回复
对于复杂场景，在7️⃣-3️⃣中间再加一点逻辑
7️⃣用户输入，加一个llm或者小模型做意图分析，意图分析的结果可以指向某些特殊工具，然后将结果再拼接到5️⃣中，6️⃣的llm也可以拉一条线出来挂function之类的东西
基本上最复杂的智能体也就这样了，大部分ai应用都是只有这个的一部分逻辑
比如说多模态输入，用户输入图片，视频，声音，就根据文件后缀调用对应的识别函数或者小模型，将信息提取出来
涉及到llm+多个特化小模型的就是moe架构，混合专家模型，相当于一个llm充当人机交互层，然后意图分析层将信息路由到对应的小模型，处理后在加上数据层RAG的召回结果，提交给决策层LLM回答
moe架构算是当前主流架构了，gpt5就是这个架构，不过几乎目前商用的模型都离不开这个架构


负责数据平台数据引擎功能的开发和维护保障数据平台自动驾驶相关数据生成和数据处理的效率
.使用argo workflow任务编排自动化，确保各项数据工作流的功能正常迭代。
3.负责跟进k8s集群资源的稳定可靠，处理集群异常问题。
任职要求
需要具有三年及以上后端开发经验，并熟练使用Python语言开发
1.精通 Django 框架，熟悉其底层机制(如ORM、中间件、信号、Admin 扩展等)。2.熟悉 Django REST Framework，具备前后端分离项目经验，有对接k8s和argo相关开发经验
羊修于ApSscheduler等异步与并发编程技术，
加分项:
1.具备自动驾驶数据处理经验，比如ros2数据解析C++相关代码的编写，了解雷达-相机融合原理。
2.熟悉消息队列(如 Kafka、RabbitMQ.Redis Streams)
3.有云原生(Cloud Native)项目经验，熟悉Prometheus、Grafana 监控体系。4.有前端vue2/ueㄋ开发能力。